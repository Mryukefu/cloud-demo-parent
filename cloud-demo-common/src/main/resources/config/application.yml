spring:
  redis:
    # Redis数据库索引（默认为0）
    database: 0
    # Redis服务器地址
    host: 127.0.0.1
    # Redis服务器连接端口
    port: 6379
      # Redis服务器连接密码（默认为空）
    # 连接超时时间（毫秒）
    timeout: 10000ms
    lettuce:
      pool:
        min-idle: 2
        max-active: 30
        max-idle: 30
